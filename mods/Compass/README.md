# Compass

Early, read-only UMG prototype. `Ctrl+F3` toggles a per-player overlay that points to the nearest loaded `BP_Collectable_Gear_C` actor using a relative direction and metric distance.

This first version intentionally targets only Gears that are present in the current level. It does not yet match every collectible category against save data.
